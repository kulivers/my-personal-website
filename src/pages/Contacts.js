import styled from "styled-components";

const Container = styled.section`
  height: 1500px;
  background-color: rgba(208, 0, 255, 0.41);
`;

const Contacts = (props) => {
  return <Container id="contacts">Contacts</Container>;
};
export default Contacts;
