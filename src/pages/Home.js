import styled from "styled-components";

const Container = styled.section`
  height: 1500px;
  background-color: rgba(255, 9, 0, 0.41);
`;

const Home = (props) => {
  return <Container id="home">Home</Container>;
};
export default Home;
